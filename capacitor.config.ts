import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.mobileflow.mobilehelloworld',
  appName: 'mobile-hello-world',
  webDir: 'dist/mobile-hello-world/browser'
};

export default config;
